# Dependencies are added milestone by milestone so you can see why each one
# is here. M1 (ingestion) only needs an HTTP client.
requests>=2.31

# M2 -- Reader agent: define typed schemas + call Claude for structured output
pydantic>=2.6
anthropic>=0.40

# Added in later milestones:
#   M3 Validator agent  -> omnipath, pandas
#   M5 Review UI        -> streamlit
