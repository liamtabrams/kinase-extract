# Dependencies are added milestone by milestone so you can see why each one
# is here. M1 (ingestion) only needs an HTTP client.
requests>=2.31

# M2 -- Reader agent: define typed schemas + call Claude for structured output
pydantic>=2.6
anthropic>=0.40
python-dotenv>=1.0   # loads your ANTHROPIC_API_KEY from a local .env file

# M3 -- Validator agent: cross-check triples against curated data
omnipath>=1.0
pandas>=2.0

# M5 -- Human-in-the-loop review UI
streamlit>=1.30
